services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: audit
      POSTGRES_USER: audit
      POSTGRES_PASSWORD: audit
    ports:
      - "5432:5432"
    volumes:
      - audit-postgres:/var/lib/postgresql/data
volumes:
  audit-postgres:
